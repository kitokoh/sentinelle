# URL de base consommée par le client axios.
# Par défaut '/api' :
#   - en dev, le serveur Vite proxifie /api -> http://localhost:8000 (voir vite.config.ts) ;
#   - en prod (image Docker), nginx proxifie /api/ -> http://api:8000/api/ (voir nginx.conf).
VITE_API_BASE_URL=/api
