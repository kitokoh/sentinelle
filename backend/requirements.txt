# Sentinelle backend — pinned dependencies (Python 3.10+)
fastapi==0.115.6
uvicorn==0.32.1
sqlmodel==0.0.22
pydantic==2.10.4
pydantic-settings==2.7.0

# HTTP client (runtime dep since v0.2 — NVD CVE enrichment)
httpx==0.27.2

# Database drivers
aiosqlite==0.20.0
asyncpg==0.30.0

# Auth (bcrypt pinned <4.1 to avoid the passlib/bcrypt>=4.1 breakage)
PyJWT==2.10.1
passlib==1.7.4
bcrypt==4.0.1

# Job queue (arq worker + redis)
arq==0.26.0
redis==4.6.0

# Tests
pytest==8.3.4
pytest-asyncio==0.24.0

# Schema migrations (v0.3 — issue #4)
alembic==1.14.0

# Declarative detection rules (v0.3 — issue #2)
PyYAML==6.0.2

# PDF audit reports (v0.5 — issue #11). Pure Python: no pango/cairo in the image.
fpdf2==2.8.2

# OIDC / Keycloak authorization-code flow (v0.5 — issue #14)
authlib==1.4.0
